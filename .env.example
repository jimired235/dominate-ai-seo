# Email Configuration (Required for contact form and audit notifications)
EMAIL_FROM=your-email@gmail.com
EMAIL_PASSWORD=your-app-password
EMAIL_TO=notifications@dominate-ai-seo.com

# Database Configuration (Optional - uses in-memory storage by default)
DATABASE_URL=postgresql://username:password@localhost:5432/aiseo_db

# OpenAI Configuration (Required for AI audit functionality)
OPENAI_API_KEY=your-openai-api-key-here

# Application Configuration
NODE_ENV=production
PORT=5000